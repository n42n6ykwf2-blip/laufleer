# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# namuna fayl commit qilinadi — unda faqat placeholder qiymatlar bor
!.env.local.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel
.env*

# namuna fayl commit qilinadi — unda faqat placeholder qiymatlar bor
# (bu qator .env* dan KEYIN turishi shart — gitignore'da oxirgi mos shablon g'olib)
!.env.local.example

# agent skillari — mazmuni commit qilinmaydi, skills-lock.json yetarli.
# Tiklash: npx skills experimental_install
.agents/
# bular absolyut symlinklar — boshqa kompyuterda buziladi
.claude/skills/

# Demo seed migratsiyalari ATAYIN olib tashlangan.
# OneDrive ularni tiklab qo'ysa ham, repoga qaytmasin.
supabase/migrations/0002_seed.sql
supabase/migrations/0004_seed_more.sql
